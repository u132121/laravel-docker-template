# Laravel Lesson レビュー①

## Todo一覧機能

### Todoモデルのallメソッドで実行しているSQLは何か
* select * from todos;

### Todoモデルのallメソッドの返り値は何か
* Illuminate\Database\Eloquent\Collectionインスタンス

### 配列の代わりにCollectionクラスを使用するメリットは
* メソッドチェーンにてfilterメソッドなど便利なメソッドを使える。

### view関数の第1・第2引数の指定と何をしているか
* 第1引数：画面に表示したいbladeファイルの指定
* 第2引数：bladeファイルに渡したいデータの連想配列の指定

### index.blade.phpの$todos・$todoに代入されているものは何か
* $todos配列の中にオブジェクトとしてTodoモデルで取得したレコードが全て代入されている。
* $todoオブジェクトの中にTodoモデルで取得したレコードが1行代入されている。

## Todo作成機能

### Requestクラスのallメソッドは何をしているか
* formから送信された値を全て一括で取得している。

### fillメソッドは何をしているか
* $todoインスタンスの各プロパティに一括で代入している。

### $fillableは何のために設定しているか
* 悪意のあるデータを一括代入で登録させないため。（今回だとcontent以外は登録できない）

### saveメソッドで実行しているSQLは何か
* オブジェクトの状態をDBに追加するINSERT文

### redirect()->route()は何をしているか
* 処理が完了した後に遷移する画面の設定

## その他

### テーブル構成をマイグレーションファイルで管理するメリット
* SQLを知らなくても、PHPコードを使用してテーブル操作ができること。
* DBの状態を他の開発者に共有できること。

### マイグレーションファイルのup()、down()は何のコマンドを実行した時に呼び出されるのか
* up()：DBに新しいテーブル、レコード、インデックスを追加する処理を記述する。(今回はtodosテーブルに新規レコードを追加する処理が記述されている)
* down()：up()と逆の操作を実装して、テーブルの状態を戻す処理を記述する。

### Seederクラスの役割は何か
* テストデータの管理、共有。

### route関数の引数・返り値・使用するメリット
* 第1引数：URLのパス　第2引数：リクエストが来た時に実行する処理。
* 返り値：第1引数に対応するURL。
* html内にルートパスを直接記述しなくて良くなるため、保守性が上がる。

### @extends・@section・@yieldの関係性とbladeを分割するメリット
* @extends：継承する親Bladeを指定する。
* 子Bladeの@section～@endsectionで囲われている部分を、親Bladeの@yieldの箇所に代入している。
* 親Bladeに書かれるコードを共通化して再利用できるため、可読性・保守性が向上する。

### @csrfは何のための記述か
* CSRF対策のための記述。トークンが含まれたinputタグが生成されてLaravelがトークンの検証を行う。

### {{ }}とは何の省略系か
* <?php echo e(); ?>